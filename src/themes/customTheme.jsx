import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#2196f3",
        },
        background: {
            default: "#f0f0f0",
        },
        text: {
            primary: "#333"
        },
    },
});
export default theme;